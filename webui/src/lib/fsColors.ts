// Footswitch palette FS1..FS4 -- must match the device NeoPixels (kFsColors):
// red / green / blue / yellow. Shared by FootswitchBar, AssignStrip, and the FX
// tiles/icons so the colour mapping lives in exactly one place.
export const FS_COLORS = ['#ff453a', '#30d158', '#4d96ff', '#ffcc00'];
